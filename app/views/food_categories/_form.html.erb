<%= form_with(model: @category, local: true) do |form| %>
  <% if @category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@category.errors.count, "error") %> prohibited this category from being saved:</h2>

      <ul>
      <% @category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.text_field :name, id: :category_name, class: "form-control",
    placeholder: "", autofocus: true, autocomplete: "off" %>
  </div>

  <div class="form-group">
    <%= form.text_area :description, id: :description, rows: 5, class: "form-control",
    placeholder: "",
    autofocus: true %>
  </div>

  <div class="form-group">
    <%= form.text_field :image, id: :image, class: "form-control",
    placeholder: "image", autofocus: true, autocomplete: "off" %>
  </div>
  <div class="form-group">
    <%= form.text_field :colour, id: :colour, class: "form-control",
    placeholder: "colour", autofocus: true, autocomplete: "off" %>
  </div>


  <div class="actions mt-5">
    <%= link_to 'Dashboard', dashboard_path %>
    <% if @category.name %>
     | <%= link_to 'New', new_food_category_path %> |
      <%= link_to 'View', @category %> |
      <%= link_to 'Delete', food_category_path(@category), class: "danger" , method: :delete, data: {confirm: "Are you sure you want to delete the category?"} %>
    <% end %>
    <%= form.submit 'Submit →', class: "btn btn-outline-dark float-right" %>
  </div>
<% end %>
